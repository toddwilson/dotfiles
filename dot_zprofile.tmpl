# .zprofile: (Zsh) Login shell configuration, runs once per login session.

# Use .profile when possible for cross-compatibility with bash
if [ -f "$HOME/.profile" ]; then
  source "$HOME/.profile"
fi
